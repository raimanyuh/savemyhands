// Throwaway prototype — visual sketch of the side-by-side recorder layout.
// Not linked from anywhere; live at /sandbox/recorder-side. Delete the
// `app/sandbox/` directory + `components/sandbox/` when the real layout
// ships.

import RecorderSidePrototype from "@/components/sandbox/RecorderSidePrototype";

export default function Page() {
  return <RecorderSidePrototype />;
}
